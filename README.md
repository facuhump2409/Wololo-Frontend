Correr docker-compose (para levantar toda la app)
```
$ docker-compose build && docker-compose up -d
```

Para levantar la app se hace en dos simples pasos

1- Bajar el start_app.sh

2- Correr el script y listo :)

```
$ ./start_app.sh
```
